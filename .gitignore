# Secrets — never commit.
# Config files reference secrets by variable name; values live only in .env
# on this box and are never version-controlled.
.env
*.env

# Editor and OS noise
*.swp
*~
.DS_Store

# Homepage writes runtime logs into its config dir
homepage/config/logs/
